/*
 * theprinttrade/flarum-printtrade-theme — forum entry
 *
 * Reserved for follow-up work: eyebrow label injection on
 * IndexPage section heads, dot separator rendering in meta
 * rows, branded empty-state copy.
 */

import app from 'flarum/forum/app';

app.initializers.add('theprinttrade-flarum-printtrade-theme', () => {
  // intentionally empty in v0
});
